server:
  port: 8081
  servlet:
    context-path: /

spring:
    activemq:
      broker-url: tcp://localhost:61616
      user: admin
      password: admin


amq:
  service:
    queue:
      input: module.input.email

logging:
  level:
    root: info